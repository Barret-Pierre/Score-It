import { Flip7Engine } from '@/domains/engine/flip7.engine';
import { GameEngine } from '@/domains/models/engine.model';

export const GAME_ENGINES: GameEngine[] = [{ id: '1', engine: Flip7Engine, gameId: '1' }];
